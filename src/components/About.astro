<script>
	window.addEventListener("preloader:done", () => {
		const typingEl = document.getElementById("easter-egg") as HTMLElement | null
		if (!typingEl) return
		const phrases = JSON.parse(typingEl.dataset.phrases || "[]")
		if (!phrases.length) return
		let phraseIdx = 0,
			charIdx = 0,
			isDeleting = false

		function typeLoop() {
			const current = phrases[phraseIdx]
			if (!isDeleting) {
				typingEl!.textContent = current.substring(0, charIdx + 1)
				charIdx++
				if (charIdx === current.length) {
					setTimeout(() => {
						isDeleting = true
						typeLoop()
					}, 3000)
					return
				}
				setTimeout(typeLoop, 40 + Math.random() * 30)
			} else {
				typingEl!.textContent = current.substring(0, charIdx - 1)
				charIdx--
				if (charIdx === 0) {
					isDeleting = false
					phraseIdx = (phraseIdx + 1) % phrases.length
					setTimeout(typeLoop, 500)
					return
				}
				setTimeout(typeLoop, 20)
			}
		}
		setTimeout(typeLoop, 1000)
	})

	// Tilt 3D Effect for glass-cards
	const initTilt = () => {
		if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return

		const cards = document.querySelectorAll(".about-photo-wrapper, .skill-card")

		cards.forEach((card) => {
			const el = card as HTMLElement
			let rafId: number | null = null

			el.addEventListener("mouseenter", () => {
				el.style.willChange = "transform"
			})

			el.addEventListener("mousemove", (e: Event) => {
				if (rafId) return
				rafId = requestAnimationFrame(() => {
					const mouseEvent = e as MouseEvent
					const rect = el.getBoundingClientRect()
					const x = mouseEvent.clientX - rect.left
					const y = mouseEvent.clientY - rect.top
					const centerX = rect.width / 2
					const centerY = rect.height / 2
					const rotateX = (y - centerY) / 20
					const rotateY = (centerX - x) / 20

					el.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale3d(1.02, 1.02, 1.02)`
					rafId = null
				})
			})

			el.addEventListener("mouseleave", () => {
				if (rafId) cancelAnimationFrame(rafId)
				rafId = null
				el.style.transform = "perspective(1000px) rotateX(0deg) rotateY(0deg) scale3d(1, 1, 1)"
				el.style.willChange = "auto"
			})
		})
	}

	initTilt()
</script>

<section id="sobre-mi" class="about section">
	<div class="container">
		<div class="section-title">
			<h2>Sobre <span class="gradient-text">mí</span></h2>
			<p>La historia detrás del código que escribo</p>
		</div>

		<div class="about-grid">
			<div class="about-photo-wrapper glass-card">
				<div class="about-photo">
					<div class="photo-wrapper">
						<div class="photo-frame">
							<img src="/profile.png" alt="Kevin Villatoro" class="photo-img" loading="lazy" />
							<div class="photo-glow"></div>
							<div class="corner corner-tl"></div>
							<div class="corner corner-tr"></div>
							<div class="corner corner-bl"></div>
							<div class="corner corner-br"></div>
							<div class="experience-badge">
								<span class="exp-number">10+</span>
								<span class="exp-label">años</span>
							</div>
						</div>
						<div class="easter-egg-container">
							<p
								class="easter-egg"
								id="easter-egg"
								data-phrases={JSON.stringify([
									"Un buen desarrollador es aquel que encuentra soluciones a los problemas antes de que surjan.",
									"La tecnología es solo una herramienta; las personas son quienes construyen el futuro.",
									"El mejor código es el que resuelve problemas reales, no el que impresiona en GitHub.",
								])}
							>
							</p>
						</div>
					</div>

					<div class="looking-for">
						<h3 class="looking-for-title">Lo que busco</h3>
						<div class="looking-for-list">
							<div class="looking-for-item" data-color="cyan">
								<svg
									xmlns="http://www.w3.org/2000/svg"
									width="14"
									height="14"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2"
									stroke-linecap="round"
									stroke-linejoin="round"
									aria-hidden="true"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"></polyline></svg
								>
								<span class="looking-for-label"
									>Roles sugeridos: Backend · Frontend · AI Engineer · Database Administrator</span
								>
							</div>
							<div class="looking-for-item" data-color="purple">
								<svg
									xmlns="http://www.w3.org/2000/svg"
									width="14"
									height="14"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2"
									stroke-linecap="round"
									stroke-linejoin="round"
									aria-hidden="true"
									><circle cx="12" cy="12" r="10"></circle><line x1="2" y1="12" x2="22" y2="12"></line><path
										d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"
									></path></svg
								>
								<span class="looking-for-label">Modalidad: Remoto preferente · Abierto a Híbrido/Presencial</span>
							</div>
							<div class="looking-for-item" data-color="green">
								<svg
									xmlns="http://www.w3.org/2000/svg"
									width="14"
									height="14"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2"
									stroke-linecap="round"
									stroke-linejoin="round"
									aria-hidden="true"
									><circle cx="12" cy="12" r="10"></circle><polyline points="12 6 12 12 16 14"></polyline></svg
								>
								<span class="looking-for-label">Disponibilidad: Tiempo completo · Inmediata</span>
							</div>
							<div class="looking-for-item" data-color="magenta">
								<svg
									xmlns="http://www.w3.org/2000/svg"
									width="14"
									height="14"
									viewBox="0 0 24 24"
									fill="none"
									stroke="currentColor"
									stroke-width="2"
									stroke-linecap="round"
									stroke-linejoin="round"
									aria-hidden="true"
									><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path><circle cx="12" cy="7" r="4"></circle></svg
								>
								<span class="looking-for-label">Industria: Abierto a cualquier sector</span>
							</div>
						</div>
					</div>
				</div>
			</div>

			<div class="about-content">
				<p class="about-bio">
					Tech Lead con 10+ años construyendo software que genera impacto real. Lideré el ecosistema Fintech de ShopShop durante
					casi 6 años, gestionando desde la arquitectura hasta el despliegue de soluciones de pago que procesaron millones en
					transacciones.
				</p>

				<p class="about-bio">
					Automatizé el 90% de operaciones fallidas que antes requerian una intervención manual y aceleré el ciclo de venta en un
					30%. Hoy aplico metodologías basadas en LLMs y Spec-Driven Development para entregar código robusto, escalable y
					documentado desde el diseño.
				</p>

				<div class="skills-grid">
					<div class="skill-card glass-card">
						<div class="skill-icon">
							<svg
								xmlns="http://www.w3.org/2000/svg"
								width="24"
								height="24"
								viewBox="0 0 24 24"
								fill="none"
								stroke="currentColor"
								stroke-width="2"
								stroke-linecap="round"
								stroke-linejoin="round"
								aria-hidden="true"
								><path d="M12 2L2 7l10 5 10-5-10-5z"></path><path d="M2 17l10 5 10-5"></path><path d="M2 12l10 5 10-5"
								></path></svg
							>
						</div>
						<h3 class="skill-title">Arquitectura</h3>
						<p class="skill-desc">
							APIs escalables, integraciones de punto a punto, integración de pasarelas de pago y facturación electrónica
						</p>
					</div>

					<div class="skill-card glass-card">
						<div class="skill-icon">
							<svg
								xmlns="http://www.w3.org/2000/svg"
								width="24"
								height="24"
								viewBox="0 0 24 24"
								fill="none"
								stroke="currentColor"
								stroke-width="2"
								stroke-linecap="round"
								stroke-linejoin="round"
								aria-hidden="true"
								><rect x="3" y="3" width="7" height="7"></rect><rect x="14" y="3" width="7" height="7"></rect><rect
									x="14"
									y="14"
									width="7"
									height="7"></rect><rect x="3" y="14" width="7" height="7"></rect></svg
							>
						</div>
						<h3 class="skill-title">DevOps</h3>
						<p class="skill-desc">Docker, CI/CD, despliegue portátil, documentación técnica con OpenAPI/Swagger</p>
					</div>

					<div class="skill-card glass-card">
						<div class="skill-icon">
							<svg
								xmlns="http://www.w3.org/2000/svg"
								width="24"
								height="24"
								viewBox="0 0 24 24"
								fill="none"
								stroke="currentColor"
								stroke-width="2"
								stroke-linecap="round"
								stroke-linejoin="round"
								aria-hidden="true"
								><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path><circle cx="9" cy="7" r="4"></circle><path
									d="M23 21v-2a4 4 0 0 0-3-3.87"></path><path d="M16 3.13a4 4 0 0 1 0 7.75"></path></svg
							>
						</div>
						<h3 class="skill-title">Liderazgo</h3>
						<p class="skill-desc">Gestión de equipos, Scrum/Kanban, toma de decisiones técnicas, mentoria de desarrollo</p>
					</div>
				</div>

				<div class="edu-card glass-card">
					<div class="edu-icon">
						<svg
							xmlns="http://www.w3.org/2000/svg"
							width="20"
							height="20"
							viewBox="0 0 24 24"
							fill="none"
							stroke="currentColor"
							stroke-width="2"
							stroke-linecap="round"
							stroke-linejoin="round"
							aria-hidden="true"><path d="M22 10v6M2 10l10-5 10 5-10 5z"></path><path d="M6 12v5c3 3 6 3 12 0v-5"></path></svg
						>
					</div>
					<div class="edu-info">
						<span class="edu-title">Ingeniería en Sistemas de Información y Ciencias de la Computación</span>
						<span class="edu-place">Universidad Mariano Gálvez de Guatemala</span>
						<div class="edu-progress">
							<div class="edu-progress-bar">
								<div class="edu-progress-fill"></div>
							</div>
							<span class="edu-progress-label">5° semestre</span>
						</div>
					</div>
				</div>
			</div>
		</div>

		<div class="section-cta">
			<a href="#trayectoria" aria-label="Ir a trayectoria">
				<svg
					xmlns="http://www.w3.org/2000/svg"
					viewBox="0 0 24 24"
					fill="none"
					stroke="currentColor"
					stroke-linecap="round"
					stroke-linejoin="round"
					aria-hidden="true"><path d="m6 9 6 6 6-6"></path></svg
				>
			</a>
		</div>
	</div>
</section>

<style>
	.about {
		overflow: hidden;
	}

	.about-grid {
		display: grid;
		grid-template-columns: 1fr 2fr;
		gap: 4rem;
		align-items: start;
	}

	/* ── Photo Wrapper (Holographic Card) ── */
	.about-photo-wrapper {
		padding: 1.5rem;
		transform-style: preserve-3d;
		will-change: transform;
		transition: transform 0.3s ease-out;
	}

	/* ── Photo ── */
	.about-photo {
		position: relative;
	}

	.photo-wrapper {
		display: flex;
		flex-direction: column;
		align-items: center;
	}

	.photo-frame {
		position: relative;
		padding: 0;
		overflow: visible;
		width: 100%;
		aspect-ratio: 1;
	}

	.easter-egg-container {
		height: 6rem;
		display: flex;
		align-items: center;
		justify-content: center;
		padding-top: 2rem;
	}

	.easter-egg {
		font-size: 0.9rem;
		font-style: italic;
		color: var(--text-muted);
		text-align: center;
		line-height: 1.5;
		width: 100%;
	}

	.easter-egg::after {
		content: "|";
		display: inline-block;
		color: var(--accent-cyan);
		margin-left: 2px;
		animation: blink 1s step-end infinite;
		vertical-align: text-bottom;
	}

	@keyframes blink {
		0%,
		100% {
			opacity: 1;
		}
		50% {
			opacity: 0;
		}
	}

	.corner {
		position: absolute;
		width: 20px;
		height: 20px;
		border-style: solid;
		border-width: 2px;
		border-radius: 4px;
		z-index: 3;
		pointer-events: none;
	}

	.corner-tl {
		top: -12px;
		left: -12px;
		border-width: 2px 0 0 2px;
		border-color: var(--accent-cyan);
	}

	.corner-tr {
		top: -12px;
		right: -12px;
		border-width: 2px 2px 0 0;
		border-color: var(--accent-magenta);
	}

	.corner-bl {
		bottom: -12px;
		left: -12px;
		border-width: 0 0 2px 2px;
		border-color: var(--accent-magenta);
	}

	.corner-br {
		bottom: -12px;
		right: -12px;
		border-width: 0 2px 2px 0;
		border-color: var(--accent-cyan);
	}

	.photo-img {
		width: 100%;
		height: 100%;
		object-fit: cover;
	}

	.photo-glow {
		position: absolute;
		inset: 0;
		border-radius: var(--radius-lg);
		box-shadow: inset 0 0 60px rgba(0, 212, 255, 0.1);
		pointer-events: none;
	}

	.experience-badge {
		position: absolute;
		bottom: -12px;
		right: -12px;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		width: 80px;
		height: 80px;
		background: var(--accent-gradient);
		border-radius: 50%;
		z-index: 2;
	}

	.exp-number {
		font-family: var(--font-heading);
		font-size: 1.5rem;
		font-weight: 700;
		color: var(--bg-primary);
		line-height: 1;
	}

	.exp-label {
		font-size: 0.7rem;
		font-weight: 600;
		color: var(--bg-primary);
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}

	/* ── Looking For ── */
	.looking-for {
		margin-top: 1.5rem;
		padding-top: 1.5rem;
		border-top: 1px solid var(--glass-border);
		width: 100%;
	}

	.looking-for-title {
		font-size: 0.8rem;
		font-weight: 600;
		color: var(--text-muted);
		margin-bottom: 1rem;
		text-align: center;
		text-transform: uppercase;
		letter-spacing: 0.1em;
	}

	.looking-for-list {
		display: flex;
		flex-direction: column;
		gap: 0.35rem;
	}

	.looking-for-item {
		display: flex;
		align-items: center;
		gap: 0.7rem;
		padding: 0.5rem 0.7rem;
		border-left: 3px solid transparent;
		border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
	}

	[data-color="cyan"] {
		border-left-color: var(--accent-cyan);
	}
	[data-color="magenta"] {
		border-left-color: var(--accent-magenta);
	}
	[data-color="green"] {
		border-left-color: var(--accent-green);
	}
	[data-color="purple"] {
		border-left-color: var(--accent-purple);
	}

	[data-color="cyan"] svg {
		color: var(--accent-cyan);
	}
	[data-color="magenta"] svg {
		color: var(--accent-magenta);
	}
	[data-color="green"] svg {
		color: var(--accent-green);
	}
	[data-color="purple"] svg {
		color: var(--accent-purple);
	}

	.looking-for-label {
		font-size: 0.82rem;
		color: var(--text-secondary);
	}

	/* ── Content ── */
	.about-content {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	.about-bio {
		font-size: 1.1rem;
		color: var(--text-secondary);
		line-height: 1.8;
	}

	.about-bio:first-child {
		font-size: 1.2rem;
		color: var(--text-primary);
	}

	.about-recommendations {
		display: inline-flex;
		align-items: center;
		gap: 0.4rem;
		font-size: 0.9rem;
		font-weight: 500;
		color: var(--accent-magenta);
		text-decoration: none;
		transition:
			color 0.2s ease,
			gap 0.2s ease;
	}

	.about-recommendations:hover {
		color: var(--accent-cyan);
		gap: 0.6rem;
	}

	.rec-heart {
		animation: heartbeat 1.5s ease-in-out infinite;
	}

	@keyframes heartbeat {
		0%,
		100% {
			transform: scale(1);
		}
		15% {
			transform: scale(1.2);
		}
		30% {
			transform: scale(1);
		}
		45% {
			transform: scale(1.15);
		}
		60% {
			transform: scale(1);
		}
	}

	@media (max-width: 968px) {
		.about-recommendations {
			justify-content: center;
		}
	}

	/* ── Skills ── */
	.skills-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1rem;
		margin-top: 0.5rem;
	}

	.skill-card {
		padding: 1.5rem;
		text-align: center;
		transition:
			transform 0.3s ease-out,
			box-shadow 0.3s ease;
		transform-style: preserve-3d;
		will-change: transform;
	}

	.skill-icon {
		width: 48px;
		height: 48px;
		display: flex;
		align-items: center;
		justify-content: center;
		margin: 0 auto 1rem;
		background: rgba(0, 212, 255, 0.1);
		border-radius: var(--radius-md);
		color: var(--accent-cyan);
		transition:
			background 0.3s ease,
			color 0.3s ease;
	}

	.skill-card:hover .skill-icon {
		background: rgba(0, 212, 255, 0.2);
		color: var(--accent-cyan);
	}

	.skill-card:nth-child(2) .skill-icon {
		background: rgba(255, 0, 170, 0.1);
		color: var(--accent-magenta);
	}

	.skill-card:nth-child(2):hover .skill-icon {
		background: rgba(255, 0, 170, 0.2);
	}

	.skill-card:nth-child(3) .skill-icon {
		background: rgba(52, 211, 153, 0.1);
		color: var(--accent-green);
	}

	.skill-card:nth-child(3):hover .skill-icon {
		background: rgba(52, 211, 153, 0.2);
	}

	.skill-title {
		font-size: 1rem;
		margin-bottom: 0.5rem;
		color: var(--text-primary);
	}

	.skill-desc {
		font-size: 0.85rem;
		color: var(--text-muted);
		line-height: 1.5;
	}

	/* ── Education ── */
	.edu-card {
		display: flex;
		align-items: flex-start;
		gap: 1rem;
		padding: 1.25rem 1.5rem;
		margin-top: 0.5rem;
		transform-style: preserve-3d;
		will-change: transform;
		transition: transform 0.3s ease-out;
	}

	.edu-icon {
		flex-shrink: 0;
		display: flex;
		align-items: center;
		justify-content: center;
		width: 40px;
		height: 40px;
		background: rgba(168, 85, 247, 0.1);
		border: 1px solid rgba(168, 85, 247, 0.15);
		border-radius: var(--radius-md);
		color: var(--accent-purple);
	}

	.edu-info {
		flex: 1;
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
		min-width: 0;
	}

	.edu-title {
		font-size: 0.9rem;
		font-weight: 600;
		color: var(--text-primary);
		line-height: 1.3;
	}

	.edu-place {
		font-size: 0.8rem;
		color: var(--accent-magenta);
	}

	.edu-progress {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		margin-top: 0.5rem;
	}

	.edu-progress-bar {
		flex: 1;
		height: 4px;
		background: rgba(255, 255, 255, 0.06);
		border-radius: 2px;
		overflow: hidden;
	}

	.edu-progress-fill {
		width: 42%;
		height: 100%;
		background: var(--accent-gradient);
		border-radius: 2px;
		animation: progressPulse 3s ease-in-out infinite;
	}

	@keyframes progressPulse {
		0%,
		100% {
			opacity: 1;
		}
		50% {
			opacity: 0.7;
		}
	}

	.edu-progress-label {
		font-size: 0.7rem;
		font-weight: 600;
		color: var(--text-muted);
		white-space: nowrap;
	}

	/* ── Responsive ── */
	@media (max-width: 968px) {
		.about-grid {
			grid-template-columns: 1fr;
			gap: 2.5rem;
			text-align: center;
		}

		.about-photo {
			display: flex;
			flex-direction: column;
			align-items: center;
		}

		.photo-wrapper {
			position: static;
		}

		.looking-for-item {
			text-align: left;
		}

		.skills-grid {
			grid-template-columns: repeat(3, 1fr);
		}
	}

	@media (max-width: 640px) {
		.skills-grid {
			grid-template-columns: 1fr;
		}

		.experience-badge {
			width: 60px;
			height: 60px;
			bottom: -6px;
			right: -6px;
		}

		.exp-number {
			font-size: 1.1rem;
		}

		.exp-label {
			font-size: 0.55rem;
		}
	}
</style>
